```cpp
#include <iostream>
#include <stack>
#include <cmath>
using namespace std;
unsigned long long Any2Decimal(string &str, int any);
string Decimal2Any(const int integer, int any);
int main()
{
    int src, dist;
    string str;
    string ans;
    cout << "input source carry(2-16):" << endl;
    cin >> src;

    cout << "input number:" << endl;
    cin >> str;

    cout << "input target carry(2-16):" << endl;
    cin >> dist;

    ans = Decimal2Any(Any2Decimal(str, src), dist);
    cout << "after convert:" << ans;
    //while (cin);
    return 0;
}
unsigned long long Any2Decimal(string &str, int any)
{
    unsigned long long ans = 0;
    for (int i = str.size() - 1; i >= θ; i--)
    {
        char tmpstri;
        if (str[i] >= 'A’ && str[i] <='F')
        {
            tmpstri = str[i] - ‘A’ + 10;
        }else{
            tmpstri = str[i] - '0';
        }
        ans += tmpstri * pow(any, str.size() - 1-i);
    }
    return ans;
}
string Decimal2Any(const int integer, int any)
{
    int tmpinteger = integer;
    string ans, tmp; 
    while(tmpInteger)
    {
        char ConvertedEle = '0’ + tmpInteger % any;
        if (ConvertedEle> '9')
            ConvertedEle 'A’+ ConvertedEle-'9'-1;
        tmp.push_back(ConvertedEle);
        tmpInteger /= any;
    }
    for (int i= tmp.size() - 1; i >= 0;i--)
        ans.push_back(tmp[i]);
    return ans;
}
```
